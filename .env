# --- WordPress 配置 ---
# --- WordPress Configuration ---
WP_ROOT="/var/www/blog"

# --- SQLite 插件版本 ---
# --- SQLite Plugin Version ---
SQLITE_VERSION="2.2.17"

# --- 本地备份配置 ---
# --- Local Backup Configuration ---
# 备份存放的临时目录
# Temporary directory for storing backups
LOCAL_BACKUP_DIR="/var/www/wp_backups"
# 统一的备份文件名（用于简单导入/导出）
# Standard backup file name (for easy import/export)
EXPORT_NAME="wp_essential_data.tar.gz"

# --- Cloudflare R2 / rclone 配置 ---
# 在 rclone config 中设置的名字
# Name set in rclone config
RCLONE_REMOTE="r2_storage"
# R2 存储桶中的文件夹名
# Folder name in the R2 bucket
REMOTE_DIR="my-blog-storage/backups"
# 云端备份保留天数
# Cloud backup retention days
RETENTION_DAYS=7
